import { BizzLogicPipe } from './bizz-logic.pipe';

describe('BizzLogicPipe', () => {
  it('create an instance', () => {
    const pipe = new BizzLogicPipe();
    expect(pipe).toBeTruthy();
  });
});
